name: Build Windows NSIS installer (manual)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-installer:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version and installer name
        id: get_version
        shell: pwsh
        run: |
          $pkg = Get-Content package.json -Raw | ConvertFrom-Json
          $version = $pkg.version
          if (-not $version) { $version = "0.0.0" }
          $installer = "oc-custom-setup-x64-$version.exe"
          echo "version=$version" >> $env:GITHUB_OUTPUT
          echo "installer_name=$installer" >> $env:GITHUB_OUTPUT

      - name: Ensure Bun is installed (bash)
        shell: bash
        run: |
          if ! command -v bun >/dev/null 2>&1; then
            curl -fsSL https://bun.sh/install | bash
            export BUN_INSTALL="$HOME/.bun"
            export PATH="$BUN_INSTALL/bin:$PATH"
          fi
          bun --version || true

      - name: Install dependencies and build (bun)
        shell: bash
        run: |
          export BUN_INSTALL="$HOME/.bun"
          export PATH="$BUN_INSTALL/bin:$PATH"
          bun install
          bun build

      - name: Prepare installer files (bash)
        shell: bash
        run: |
          OUTPUT_DIR="dist"
          if [ ! -d "$OUTPUT_DIR" ]; then
            echo "Warning: $OUTPUT_DIR does not exist. Listing repo:" && ls -la
          fi
          rm -rf installer_files || true
          mkdir -p installer_files
          if [ -d "$OUTPUT_DIR" ]; then
            cp -r "$OUTPUT_DIR"/* installer_files/ || true
          fi
          # 可选：包含 bun runtime（使安装器更自包含）
          if command -v bun >/dev/null 2>&1; then
            BUN_BIN=$(command -v bun)
            mkdir -p installer_files/bin
            cp "$BUN_BIN" installer_files/bin/ || true
          fi

      - name: Install NSIS (choco)
        shell: pwsh
        run: |
          choco install nsis -y

      - name: Create NSIS script (bash heredoc + placeholder replacement)
        shell: bash
        run: |
          installer="${{ steps.get_version.outputs.installer_name }}"
          app="oc-custom"

          mkdir -p installer_files || true

          cat > installer.nsi <<'NSIS'
Name "oc-custom Installer"
OutFile "${INSTALLER_PLACEHOLDER}"
InstallDir "$PROGRAMFILES64\oc-custom"
Page directory
Page instfiles
Section "Install"
  SetOutPath "$INSTDIR"
  File /r "installer_files\*"
  CreateShortCut "$SMPROGRAMS\oc-custom.lnk" "$INSTDIR\oc-custom.exe"
SectionEnd
NSIS

          # replace placeholder with actual installer filename
          sed -i "s|\${INSTALLER_PLACEHOLDER}|$installer|g" installer.nsi

      - name: Build installer with makensis
        shell: pwsh
        run: |
          $installerName = "${{ steps.get_version.outputs.installer_name }}"
          & "C:\Program Files (x86)\NSIS\makensis.exe" installer.nsi
          if (-not (Test-Path $installerName)) { Write-Error "Installer not created: $installerName" }

      - name: List generated files
        shell: pwsh
        run: |
          Get-ChildItem -Path . -Recurse -Force | Where-Object { $_.Length -gt 0 } | Select-Object FullName, Length | Format-Table -AutoSize

      - name: Create GitHub Release and upload installer
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ steps.get_version.outputs.version }}
          files: ${{ steps.get_version.outputs.installer_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_FOR_SYNC }}
